@{
    Layout = null;
}
@model AssetStatusDTO
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">İptal Et</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close" id="btnClose">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <form asp-action="ToCancel" asp-controller="AssetAction" method="post">
                @Html.HiddenFor(x => x.AssetID)
                @Html.HiddenFor(x => x.StatusID)
                <div class="modal-body">
                    <div class="form-group">
                        <label class="lbl-asset"><label class="reqSpan">*</label> Açıklama</label>
                        <textarea asp-for="@Model.Note" class="form-control" rows="3"></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary" style="background-color: #1753c9">
                        <i class="fas fa-angle-right fa-fw"> </i>İptal Et
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
